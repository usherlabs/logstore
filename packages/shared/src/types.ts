export enum Manager {
	NodeManager = 'NodeManager',
	StoreManager = 'StoreManager',
	QueryManager = 'QueryManager',
}

export enum Network {
	Local = 5,
	Dev = 8997,
	Testnet = 80001,
	Mainnet = 137,
}
